// Placeholder for future interactivity, e.g., dynamically loading sample cards
// You could host wav files under docs/samples/ and add them here.

document.addEventListener("DOMContentLoaded", () => {
  // Example: dynamically add sample cards if you have a samples.json
  // fetch('samples/samples.json').then(r => r.json()).then(samples => { ... })
});

